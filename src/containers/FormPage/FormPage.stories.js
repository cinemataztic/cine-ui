import React from 'react';
import Form from './FormPage.container.js';

export default {
  title: 'Example/Form',
  component: Form,
};

const Template = () => <Form />;

export const FormComponent = Template.bind({});
